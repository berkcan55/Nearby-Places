import React, { Component } from 'react';
import { View, Text } from 'react-native';
import Hospital from '../../components/Map/Hospital'

export default function HospitalScreen({ route, navigation }) {
    return(
        <Hospital />
    )
}
